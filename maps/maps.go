
package main
/*
import "fmt"

type data struct {
	Name  string
	age   int
	email string
}

func main() {
	mapData := make(map[string]data)

	var n int
	fmt.Println("Enter the number of elements to be in map:")
	fmt.Scan(&n)

	for i := 0; i < n; i++ {
		var userName, Name, email string
		var age int

		fmt.Print("Enter userName: ")
		fmt.Scan(&userName)

		fmt.Print("Enter Name: ")
		fmt.Scan(&Name)

		fmt.Print("Enter age: ")
		fmt.Scan(&age)

		fmt.Print("Enter email: ")
		fmt.Scan(&email)

		mapData[userName] = data{Name: Name, age: age, email: email}
	}

	fmt.Println("\nMap Data:", mapData)

	var searchItem string
	fmt.Print("\nEnter username to search: ")
	fmt.Scan(&searchItem)

	exists, ok := mapData[searchItem]
	if ok {
		fmt.Println("User found:", exists)
	} else {
		fmt.Println("User not found")
	}
}
*/